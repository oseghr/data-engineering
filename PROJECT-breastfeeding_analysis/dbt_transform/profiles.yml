breastfeeding_analytics:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: "{{ env_var('GCP_PROJECT') }}"
      dataset: breastfeeding_analytics
      location: US
      threads: 4
      timeout_seconds: 300
